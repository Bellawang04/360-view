import BaseController from "./BaseController";

/**
 * @namespace com.yiqian.customer360.controller
 */
export default class Main extends BaseController {
	// Redirected to Launchpad - kept for routing fallback
}
